// import React from "react";

// // Connect to your server

// const CallButton = ({ receiverId }) => {
//   const initiateCall = () => {
//     const meetingId = "123"; // Generate or fetch this dynamically
//     socket.emit("initiate_call", { receiverId, meetingId });
//   };

//   return <button onClick={initiateCall}>Call Kiran</button>;
// };

// export default CallButton;
